name: Build TEST BAN DO

on:
  workflow_dispatch:
  push:
    branches: [ main, feature/ui-direct-3-icons-correct-base ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: TEST UI DIRECT source contract
        run: |
          python -m pip install pytest
          python -m pytest -q tests/test_ui_direct_tab_contract.py
      - name: Configure x64
        run: cmake -S . -B build -A x64
      - name: Build Release
        run: cmake --build build --config Release --parallel
      - name: Smoke test main window startup
        shell: pwsh
        run: |
          $exe = Resolve-Path 'build/Release/ThanLongTestSell.exe'
          $p = Start-Process -FilePath $exe -PassThru
          try {
            $idle = $p.WaitForInputIdle(5000)
            $p.Refresh()
            if ($p.HasExited) { throw "EXE exited during startup with code $($p.ExitCode)" }
            if (-not $idle) { throw 'EXE never reached input-idle state' }
            if ($p.MainWindowHandle -eq 0) { throw 'No main window handle after startup' }
            if ($p.MainWindowTitle -ne 'TEST BÁN ĐỒ • DROP SLEEP') { throw "Unexpected main window title: $($p.MainWindowTitle)" }
            Write-Host "STARTUP PASS HWND=$($p.MainWindowHandle) TITLE=$($p.MainWindowTitle)"
          }
          finally {
            if (-not $p.HasExited) { Stop-Process -Id $p.Id -Force }
          }
      - name: Verify no fixed Sleep in ThanLongTestSell source
        shell: pwsh
        run: |
          $files = @(
            Get-ChildItem src/controller_chunks -File
            Get-ChildItem src/bridge_chunks -File | Where-Object { $_.Name -match '^bridge_(0[1-9]|1[0-3])\.txt$' }
            Get-Item src/protocol.h
            Get-Item src/internal_ui_click_logic.h
          )
          $hits = $files | Select-String -Pattern '\bSleep\s*\('
          if ($hits) { $hits | ForEach-Object { Write-Host $_ }; throw 'Sleep() found in ThanLongTestSell source' }
      - name: Stage runtime and source packages
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package,runtime_stage,source_stage | Out-Null

          Copy-Item build/Release/ThanLongTestSell.exe package/
          Copy-Item build/Release/ThanLongTestSellBridge.dll package/
          Copy-Item build/Release/test_sell.ini package/
          Copy-Item README.md package/
          Copy-Item VERSION_REPORT.md package/

          Copy-Item build/Release/ThanLongTestSell.exe runtime_stage/
          Copy-Item build/Release/ThanLongTestSellBridge.dll runtime_stage/
          Copy-Item build/Release/test_sell.ini runtime_stage/
          Copy-Item README.md runtime_stage/
          Copy-Item VERSION_REPORT.md runtime_stage/
          Compress-Archive -Path runtime_stage/* -DestinationPath package/ThanLong-Test-UI-DIRECT-3Icons-startup-fixed-win-x64.zip -Force

          Copy-Item CMakeLists.txt source_stage/
          Copy-Item README.md source_stage/
          Copy-Item VERSION_REPORT.md source_stage/
          Copy-Item test_sell.ini source_stage/
          Copy-Item src source_stage/src -Recurse
          Copy-Item tests source_stage/tests -Recurse
          New-Item -ItemType Directory -Force -Path source_stage/.github/workflows | Out-Null
          Copy-Item .github/workflows/build-test-sell.yml source_stage/.github/workflows/
          Compress-Archive -Path source_stage/* -DestinationPath package/ThanLong-Test-UI-DIRECT-3Icons-startup-fixed-source.zip -Force
      - name: Upload EXE and source package
        uses: actions/upload-artifact@v4
        with:
          name: ThanLong-Test-UI-DIRECT-3Icons-StartupFixed
          path: package/*
          if-no-files-found: error
